# Manual Checks

These scripts are legacy/manual checks kept for reference while the active pytest suite is consolidated under `tests/`.

They are intentionally named `*_check.py` so pytest does not collect them automatically. Prefer adding new automated coverage to the main `tests/` suite.

Sample-data checks use `examples/data/chunks.json`.
