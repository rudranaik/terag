numpy>=1.21.0
networkx>=3.0
tqdm>=4.0.0

# Optional capabilities:
#   pip install "terag[openai]"   # OpenAI NER and embeddings
#   pip install "terag[groq]"     # Groq NER
#   pip install "terag[local]"    # SentenceTransformers local embeddings
#   pip install "terag[bench]"    # Benchmark harness provider dependencies
#   pip install "terag[dev]"      # Tests and development tooling
